# Destination for the "Install AutoFA" button shown to Android mobile-browser
# users (Play Store listing, direct APK, etc). If left unset, the install
# recommendation still renders but the install action is disabled rather
# than pointing at a broken/fake link — see src/lib/platform.js.
VITE_ANDROID_APP_INSTALL_URL=
