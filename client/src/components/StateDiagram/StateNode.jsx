export default function StateNode({ state, point, accepting, dead, active }) { const stroke = active ? 'var(--diagram-active)' : dead ? 'var(--diagram-dead)' : 'var(--diagram-stroke)'; const fill = active ? 'var(--diagram-active-soft)' : dead ? 'var(--diagram-dead-soft)' : 'var(--diagram-node)'; return <g><title>{`State ${state}${accepting ? ', accepting state' : ''}${dead ? ', dead state' : ''}`}</title>{accepting && <circle cx={point.x} cy={point.y} r="36" fill="none" stroke={stroke} strokeWidth="2" />}<circle cx={point.x} cy={point.y} r="32" fill={fill} stroke={stroke} strokeWidth={active ? 3 : 2} /><text x={point.x} y={point.y + 5} textAnchor="middle" fill="var(--diagram-text)" className="text-[13px] font-semibold">{state}</text></g>; }