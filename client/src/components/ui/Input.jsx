export default function Input({ label, id, error, className = '', ...props }) { return <label className="block text-sm font-medium text-ink-muted dark:text-ink-darkMuted">{label && <span>{label}</span>}<input id={id} aria-invalid={Boolean(error)} className={`focus-ring mt-1.5 w-full rounded-xl border bg-surface px-3 py-2.5 text-ink placeholder:text-ink-soft hover:border-primary/40 dark:bg-surface-dark dark:text-ink-dark ${error ? 'border-danger' : 'border-line dark:border-line-dark'} ${className}`} {...props} />{error && <span className="mt-1 block text-xs font-medium text-danger">{error}</span>}</label>; }