import { Link } from 'react-router-dom';
const styles = { primary: 'bg-primary text-white shadow-sm hover:bg-primary-hover hover:shadow-[0_6px_16px_rgb(22_131_216_/_0.22)] active:scale-[0.98]', secondary: 'border border-primary/30 bg-surface text-primary hover:border-primary/55 hover:bg-primary-soft dark:bg-surface-dark dark:text-sky-300 dark:hover:bg-primary/15', ghost: 'text-ink-muted hover:bg-primary-soft hover:text-primary dark:text-ink-darkMuted dark:hover:bg-primary/15', danger: 'bg-danger text-white hover:bg-red-700' };
export default function Button({ children, variant = 'primary', className = '', to, ...props }) { const shared = `focus-ring inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition duration-200 disabled:pointer-events-none disabled:opacity-50 ${styles[variant]} ${className}`; return to ? <Link to={to} className={shared}>{children}</Link> : <button className={shared} {...props}>{children}</button>; }