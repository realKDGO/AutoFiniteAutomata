function make({ alphabet, states, transitions, acceptingStates, metadata }) { return { states, alphabet, transitions, startState: states[0], acceptingStates, metadata }; }
function counted({ alphabet, count, accepting, overflow = true, metadata }) { const states = Array.from({ length: count + 1 }, (_, index) => `l${index}`); if (overflow) states.push('overflow'); const transitions = {}; for (let index = 0; index <= count; index += 1) transitions[`l${index}`] = Object.fromEntries(alphabet.map(symbol => [symbol, index === count ? (overflow ? 'overflow' : `l${index}`) : `l${index + 1}`])); if (overflow) transitions.overflow = Object.fromEntries(alphabet.map(symbol => [symbol, 'overflow'])); return make({ alphabet, states, transitions, acceptingStates: accepting(states), metadata }); }
export const buildLengthEqualAutomaton = ({ alphabet, count }) => counted({ alphabet, count, accepting: states => [states[count]], metadata: { mode: 'lengthEqual', count } });
export const buildLengthAtLeastAutomaton = ({ alphabet, count }) => counted({ alphabet, count, overflow: false, accepting: states => [states[count]], metadata: { mode: 'lengthAtLeast', count } });
export const buildLengthAtMostAutomaton = ({ alphabet, count }) => counted({ alphabet, count, accepting: states => states.slice(0, count + 1), metadata: { mode: 'lengthAtMost', count } });
export const buildEvenLengthAutomaton = ({ alphabet }) => make({ alphabet, states: ['even', 'odd'], transitions: { even: Object.fromEntries(alphabet.map(s => [s, 'odd'])), odd: Object.fromEntries(alphabet.map(s => [s, 'even'])) }, acceptingStates: ['even'], metadata: { mode: 'evenLength' } });
export const buildOddLengthAutomaton = ({ alphabet }) => { const automaton = buildEvenLengthAutomaton({ alphabet }); return { ...automaton, acceptingStates: ['odd'], metadata: { mode: 'oddLength' } }; };
export const buildNeverAutomaton = ({ alphabet }) => make({ alphabet, states: ['never'], transitions: { never: Object.fromEntries(alphabet.map(s => [s, 'never'])) }, acceptingStates: [], metadata: { mode: 'never' } });