function make({ alphabet, states, transitions, acceptingStates, metadata }) { return { states, alphabet, transitions, startState: states[0], acceptingStates, metadata }; }
function occurrences({ alphabet, count, mode, symbol }) { const states = Array.from({ length: count + 1 }, (_, index) => `c${index}`); const capped = mode === 'atLeast'; if (!capped) states.push('overflow'); const transitions = {}; for (let index = 0; index <= count; index += 1) transitions[`c${index}`] = Object.fromEntries(alphabet.map(current => [current, current === symbol ? (index === count ? (capped ? `c${index}` : 'overflow') : `c${index + 1}`) : `c${index}`])); if (!capped) transitions.overflow = Object.fromEntries(alphabet.map(current => [current, 'overflow'])); const acceptingStates = mode === 'exact' ? [`c${count}`] : mode === 'atLeast' ? [`c${count}`] : states.slice(0, count + 1); return make({ alphabet, states, transitions, acceptingStates, metadata: { mode, count, symbol } }); }
export const buildExactOccurrencesAutomaton = args => occurrences({ ...args, mode: 'exact' });
export const buildAtLeastOccurrencesAutomaton = args => occurrences({ ...args, mode: 'atLeast' });
export const buildAtMostOccurrencesAutomaton = args => occurrences({ ...args, mode: 'atMost' });
export function buildParityOccurrencesAutomaton({ alphabet, symbol, odd = false }) { const transitions = { even: {}, odd: {} }; for (const current of alphabet) { transitions.even[current] = current === symbol ? 'odd' : 'even'; transitions.odd[current] = current === symbol ? 'even' : 'odd'; } return make({ alphabet, states: ['even', 'odd'], transitions, acceptingStates: [odd ? 'odd' : 'even'], metadata: { mode: odd ? 'oddOccurrences' : 'evenOccurrences', symbol } }); }