export function generateStateDescriptions(automaton) { return automaton.states.map(state => { const initial = state === automaton.startState; const accepting = automaton.acceptingStates.includes(state); let description = initial ? 'Initial state. Reading begins here.' : 'Tracks progress while reading the input.'; if (accepting) description += ' Accepting state: the input satisfies the language definition if it ends here.'; if (!accepting && state.toLowerCase().includes('dead')) description = 'Dead state. The required prefix can no longer be satisfied.'; return { state, initial, accepting, description }; }); }