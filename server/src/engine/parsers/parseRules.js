const mapping = { startsWith: 'prefix', doesNotStartWith: 'notPrefix', endsWith: 'suffix', doesNotEndWith: 'notSuffix', contains: 'substring', doesNotContain: 'notSubstring', lengthEqual: 'lengthEqual', lengthGreaterOrEqual: 'lengthAtLeast', lengthLessOrEqual: 'lengthAtMost', lengthGreater: 'lengthGreater', lengthLess: 'lengthLess', evenLength: 'evenLength', oddLength: 'oddLength', firstSymbol: 'firstSymbol', lastSymbol: 'lastSymbol', nthSymbol: 'nthSymbol', nthSymbolNot: 'nthSymbolNot', exactOccurrences: 'exactOccurrences', atLeastOccurrences: 'atLeastOccurrences', atMostOccurrences: 'atMostOccurrences', evenOccurrences: 'evenOccurrences', oddOccurrences: 'oddOccurrences' };
export function parseRules(conditions) { const rules = []; let pending = 'AND'; for (const item of conditions) { if (item.operator && !item.type) { pending = item.operator.toUpperCase(); continue; } const rawCount = item.count ?? item.value; rules.push({ kind: mapping[item.type], value: item.value, count: rawCount === undefined ? undefined : Number(rawCount), position: item.position === undefined ? undefined : Number(item.position), symbol: item.symbol, join: rules.length ? (item.operator?.toUpperCase() || pending) : null, sourceType: item.type }); pending = 'AND'; } return rules; }